import monster
import player
from random import choice
from status import hp
aleatory_monster = choice(monster.monsters)
print(('nickname: {} / hp: {} / mp: {}'.format(nick_name[0]["name"], nick_name[0]["hp"], nick_name[0]["mp"])))
print(aleatory_monster)
